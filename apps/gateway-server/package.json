{
  "name": "@caypo/gateway-server",
  "version": "0.1.0",
  "private": true,
  "description": "CAYPO MPP Gateway server deployment",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "start": "node dist/index.js",
    "dev": "node --watch src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@caypo/canton-gateway": "workspace:*",
    "@hono/node-server": "^1.14.0"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "typescript": "^5.8.0"
  }
}
